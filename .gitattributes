# Normalize text files; keep CRLF in the working tree (repo convention).
* text=auto eol=crlf

# Binary assets — never touch line endings.
*.mp4   binary
*.webm  binary
*.webp  binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
